# Task Plan: Issue #23 — Стабилизация кода и редизайн UI карточки настроек по стандарту dsh-clinebot

## Цель
Устранить дефекты стабильности (холодный старт, event.time, таймаут OpenRouter, модели без слэша) и обновить UI плагина до единого стандарта dsh-clinebot (ErrorBoundary, дизайн-токены, бейджи, секции, адаптивная сетка, поповер).

## Задачи
- [ ] 1. Исправление серверной части (`lib/index.js`):
  - [ ] Инициализация `byLowerId` в начальном состоянии каталога
  - [ ] Валидация `timeMs` в `slotOf(timeMs)`
  - [ ] Добавление таймаута (15с) для запросов к `openrouter.ai`
  - [ ] Поддержка моделей без префикса в `splitRoute`
  - [ ] Экспорт диагностических метаданных (`catalogModelsCount`, `fetchedAt`, `error`) в `/dsh-cost-meter/state`
- [ ] 2. Обновление клиентской части (`lib/client.js`):
  - [ ] Внедрение `createErrorBoundary()` и обёртка карточки и поповера
  - [ ] Внедрение `refreshMirrorUntilVisible(ctx)`
  - [ ] Единая CSS-система дизайн-токенов (`.dcm-section-card`, `.dcm-badge`, `.dcm-grid-2`, `.dcm-btn`, `.dcm-alert-*`)
  - [ ] Структурирование настроек по 3 логическим секциям с бейджами статуса в шапке
  - [ ] Модернизация поповера шапки на классы дизайн-системы
  - [ ] Локализация новых ключей (`ru`, `en`)
- [ ] 3. Обновление документации:
  - [ ] Актуализация `docs/design/DESIGN.md`, `README.md`, `docs/README.ru.md`
- [ ] 4. Тестирование:
  - [ ] Написание юнит-тестов `test/stability-and-visual.test.mjs`
  - [ ] Прогон всех тестов (`npm test`)
- [ ] 5. Сборка, PR, релиз v0.7.10, верификация на MiniAI и закрытие Issue #23
